What I fixed

1. Replaced the broken remote logo reference with a local JW Carpet Care SVG lockup so the header and footer always show branding.
2. Darkened the hero overlays across the site so the pages no longer look washed out.
3. Switched the commercial page image references to a local JPG fallback.
4. Rendered the key site variables into the HTML so the pages do not depend on a broken external logo URL.
5. Removed blank Instagram links.
6. Checked the local page links in the built HTML files.

Important
This version uses a local JW Carpet Care logo lockup because the exact original logo image file was not present in the ZIP and the old remote logo URL was dead.
If you upload the exact logo file later, it can be swapped in quickly.
